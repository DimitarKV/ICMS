# Ice Cream Management System

## Assumptions
- We assume that the authentication with the application happens in an external company system that simply acts as an identity provider for our application. The external system provides ICMS (Ice Cream Management System) with a unique user token that is always the same for each user. 